# `@tradeofflab/ui`

This package is reserved for reusable interface primitives and decision-workspace components shared across the web application.

It should stay aligned with the product identity:

- structured workspace
- analytical review
- traceability surfaces

It should not drift into chat-centric component design.

