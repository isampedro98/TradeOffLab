# Database Platform Choice Example

This example anchors the product's default bootstrap fixture for the current MVP.

Decision question:

`Which primary OLTP database should we standardize on for the next product phase?`

Why this example matters:

- it is technical, concrete, and broadly reusable
- it has multiple viable options with clear operational tradeoffs
- it benefits from structured assumptions and weighted criteria
- it produces a recommendation that can be challenged and revised

The full dossier and structured fixtures should live here as the implementation matures.
